<!--
根目录 CLAUDE.md 模板。三条铁律：
1. 单文件 ≤200 行（每次会话全量加载，写太长就是跟自己抢 context）。
2. 根目录只放「跨包通用约定 + 关键的坑 + 指针」，模块细节下沉到子目录 CLAUDE.md。
3. 每一行都过一遍「删除测试」：删掉这行 Claude 还会做对吗？会 → 删。
把 <尖括号> 占位符替换成你项目的真实内容，并删掉用不到的小节。
-->

# <项目名> — 给 Claude 的项目说明

## 这是什么
<一两句：项目是做什么的、技术栈一句话。例：日语学习 Web 应用，前端 Vue3+TS+Vite，后端 Node/Express。>

## 关键的坑（违反会出事，最高优先级）
- <例：生产数据库严禁直接操作；只能改 migration 文件，由 CI 执行。>
- <例：`server/.env` 内的密钥严禁读取、打印、提交。>
- <例：提交前必须本地通过 lint，CI 才会过。>

## 跨包通用约定
- <例：不可变优先——返回新对象而非原地修改。>
- <例：语义命名，禁止 handler2 / utilNew / commonV3 这类名字。>
- <例：错误必须显式处理，禁止静默吞掉。>

## 在哪干活（指针，不是细节）
- 改前端 → `cd src` 再开工，细节见 `src/CLAUDE.md`
- 改后端 → `cd server` 再开工，细节见 `server/CLAUDE.md`
- 目录速览见根目录 `CODEBASE_MAP.md`
- 团队口味/偏好见 `.claude/project-taste.md`

## 怎么验证（命令在子目录 CLAUDE.md 里，这里只放全局的）
- 全量 lint: `<命令>`　·　全量测试: `<命令>`
- 子目录有各自更快的命令，优先用子目录的，别动不动跑全量。

<!-- 复审：每 3–6 个月完整过一遍；发现 Claude 反复犯的错先查本文件是不是太长把规则淹没了，再考虑加规则。 -->
