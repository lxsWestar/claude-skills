interface:
  display_name: "OCR Skill"
  short_description: "OCR PDF/DOCX/PPTX to ezkotae v2 outputs"
  default_prompt: "Use $ocrskill to OCR this file or folder and produce Markdown, matching image files, *_images.json, layout JSON, and an optional DOCX."
